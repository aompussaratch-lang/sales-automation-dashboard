import React from "react";
import ReactDOM from "react-dom/client";
import SalesSummaryDashboard from "./sales_summary_dashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SalesSummaryDashboard />
  </React.StrictMode>
);
